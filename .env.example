# Optional local workstation secrets/proxy settings.
# Do not commit real credentials. Copy this file to .env only if a future data source requires secrets.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=127.0.0.1,localhost
CDS_API_KEY=
CAMS_API_KEY=
MARINE_API_KEY=
